## Symptoms
Server slow, app timeout, high latency

## Initial Checks
Impact, recent changes, CPU, memory, disk, processes, logs, network

## Commands
top, free -h, df -h, du -sh, ps aux, journalctl, curl, ss

## Fix/Prevention
Scale/fix process/cleanup disk/root cause notes
