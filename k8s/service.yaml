apiVersion: v1
kind: Service

metadata:
  name: devops-platform-api
  namespace: devops-platform

spec:
  type: ClusterIP

  selector:
    app: devops-platform-api

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000